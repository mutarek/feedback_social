import 'package:als_frontend/data/model/response/watch_list_model.dart';
import 'package:flutter/material.dart';

class VideoTimelineWidget extends StatelessWidget {
  final WatchListModel watch_list_model;
  int index;

  VideoTimelineWidget(this.watch_list_model, this.index);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
