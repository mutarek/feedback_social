import 'package:flutter/material.dart';

class ShowFullVideos extends StatelessWidget {
  const ShowFullVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Container(),
    );
  }
}
